name: CI

on:
  push:
    branches:
      - main
      - develop
      - feat/**
      - chore/**
  pull_request:

jobs:
  build-and-lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run linters (ESLint + Biome + Stylelint)
        run: |
          npm run lint:eslint
          npm run lint:biome
          npm run lint:styles

      - name: Build project
        run: npm run build
